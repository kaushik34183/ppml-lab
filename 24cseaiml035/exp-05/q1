def fib():
    a, b = 0, 1
    while True:
        yield a
        a, b = b, a + b

even_sum = 0
for num in fib():
    if num > 1000:
        break
    if num % 2 == 0:
        even_sum += num
print(even_sum)
